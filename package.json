{
  "name": "flashnews24",
  "version": "1.0.0",
  "description": "Native Android news application with Jetpack Compose, Retrofit, Room, FCM, and AdMob integration.",
  "scripts": {
    "build": "bash gradlew assembleDebug && bash gradlew bundleRelease",
    "dev": "bash gradlew assembleDebug"
  },
  "dependencies": {}
}
